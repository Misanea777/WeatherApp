<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="viewModel"
            type="com.endava.internship.mobile.weatherapp.ui.splashscreen.SplashViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/fragment_splash"
        style="@style/Background"
        tools:context=".ui.splashscreen.SplashFragment">

        <ImageView
            android:layout_width="@dimen/logo_width"
            android:layout_height="@dimen/logo_height"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:src="@drawable/ic_logo"
            android:id="@+id/iv_logo"
            android:contentDescription="@string/splash_logo_hint"
            android:background="@android:color/transparent" />

        <ProgressBar
            android:layout_width="@dimen/progressbar_width"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@+id/iv_logo"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:id="@+id/pb_progress"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:progress="@{viewModel.progress}"
            android:layout_marginTop="@dimen/min_margin_top"
            android:focusable="false"
            android:max="100"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>